import MainLayout from '@/layouts/MainLayout';
import React from 'react';

const Audio = () => {
    return (
        <MainLayout>

            this Audio page is protected!!!
        </MainLayout>
    );
};

export default Audio;